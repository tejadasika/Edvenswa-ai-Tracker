// Side-effect CSS imports (e.g. `import './globals.css'`) are handled by
// Next.js' build pipeline at runtime. TypeScript doesn't ship a declaration
// for them, so without this file the IDE flags it as TS2882.
declare module '*.css';
